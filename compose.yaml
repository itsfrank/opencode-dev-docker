services:
  opencode:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        OPENCODE_TAG: ${OPENCODE_TAG:-latest}
    image: opencode-dev:local
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /workspace/repos
    stdin_open: true
    tty: true
    environment:
      HOME: /tmp/opencode-home
      XDG_CONFIG_HOME: /tmp/opencode-home/.config
      XDG_DATA_HOME: /tmp/opencode-home/.local/share
    ports:
      - "4096:4096"
      - "7000-7010:7000-7010"
    volumes:
      - ${WORKSPACE_DIR:-.}:/workspace/repos
      - ${HOME}/.config/opencode:/tmp/opencode-home/.config/opencode
      - ${HOME}/.local/share/opencode:/tmp/opencode-home/.local/share/opencode
    command: ["serve", "--hostname", "0.0.0.0", "--port", "4096", "--print-logs"]
